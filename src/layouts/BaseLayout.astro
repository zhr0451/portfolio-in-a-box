---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  locale?: "ru" | "en";
}

const {
  title = "Zakhar Samokhvalov | Game Design & System Administration Portfolio",
  description = "Portfolio of Zakhar Samokhvalov, a game designer and system administrator focused on indie games, Linux administration, and frontend craft.",
  locale = "en",
} = Astro.props;

const baseUrl = import.meta.env.BASE_URL;
const fontBasePath = `${baseUrl}fonts`;
const canonicalUrl = Astro.site ? new URL(Astro.url.pathname, Astro.site) : undefined;
const previewImage = Astro.site
  ? new URL(`${baseUrl}media/photo_2026-03-15_19-53-33.jpg`, Astro.site).href
  : `${baseUrl}media/photo_2026-03-15_19-53-33.jpg`;
const fontFaceCss = [
  '@font-face {\n' +
    '  font-family: "Space Mono";\n' +
    `  src: url("${fontBasePath}/SpaceMono-Regular.ttf") format("truetype");\n` +
    "  font-weight: 400;\n" +
    "  font-style: normal;\n" +
    "  font-display: swap;\n" +
    "}",
  '@font-face {\n' +
    '  font-family: "Space Mono";\n' +
    `  src: url("${fontBasePath}/SpaceMono-Italic.ttf") format("truetype");\n` +
    "  font-weight: 400;\n" +
    "  font-style: italic;\n" +
    "  font-display: swap;\n" +
    "}",
  '@font-face {\n' +
    '  font-family: "Space Mono";\n' +
    `  src: url("${fontBasePath}/SpaceMono-Bold.ttf") format("truetype");\n` +
    "  font-weight: 700;\n" +
    "  font-style: normal;\n" +
    "  font-display: swap;\n" +
    "}",
  '@font-face {\n' +
    '  font-family: "Space Mono";\n' +
    `  src: url("${fontBasePath}/SpaceMono-BoldItalic.ttf") format("truetype");\n` +
    "  font-weight: 700;\n" +
    "  font-style: italic;\n" +
    "  font-display: swap;\n" +
    "}",
].join("\n");
---

<!doctype html>
<html lang={locale} data-lang={locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="author" content="Zakhar Samokhvalov" />
    <meta name="description" content={description} />
    {canonicalUrl && <link rel="canonical" href={canonicalUrl.href} />}
    <link rel="icon" type="image/svg+xml" href={`${baseUrl}media/favicon.svg`} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    {canonicalUrl && <meta property="og:url" content={canonicalUrl.href} />}
    <meta property="og:image" content={previewImage} />
    <meta property="og:site_name" content="Zakhar Samokhvalov Portfolio" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={previewImage} />
    <style is:inline set:html={fontFaceCss}></style>
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
